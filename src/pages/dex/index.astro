---
import { getCollection, render } from 'astro:content';
import Basic from '../../layouts/Basic.astro';

const dex = await getCollection('dex');
const { Content } = await render(dex[0]);
---

<Basic title="dex" description="presentation viewer">
		<Content />
</Basic>