---
import * as markdown from '../content/index.mdx';
import Landing from '../layouts/Landing.astro';
const { Content, frontmatter } = markdown;
---

<Landing title={frontmatter.title} description={frontmatter.description}>
  <Content />
</Landing>
