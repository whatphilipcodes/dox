---
interface Props {
  name: string;
  active?: boolean;
  href?: string;
}

const { name, active, href } = Astro.props as Props;
const computed_href = href ?? '/tags/' + name;
---

<a
  class={`dark:text-cynder-500 cursor-pointer rounded-md px-2 text-neutral-500 inset-ring 
    hover:bg-neutral-500/20 dark:hover:bg-cynder-500/20 active:bg-transparent dark:active:bg-transparent 
    ${active ? 'bg-neutral-500/20 dark:bg-cynder-500/20' : ''}`}
  href={computed_href}
>
  {name}
</a>
