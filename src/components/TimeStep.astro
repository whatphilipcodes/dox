---
interface Props {
  title: string;
  start: Date;
  due: Date;
  status: 'planned' | 'active' | 'completed';
  logEntry?: string;
  tags?: string[];
}

const { title, start, due, status, logEntry, tags } = Astro.props;
---

<section>
  <slot name='content' />
</section>
